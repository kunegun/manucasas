language: node_js
node_js:
  - "10"
cache: npm
branches:
  only:
    - master
before_script:
  - "npm global add firebase-tools"
  - "npm global add gatsby"
script:
  - "npm build"
after_success:
  - "firebase deploy --token=${FIREBASE_API_TOKEN}"
