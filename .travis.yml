language: node_js
node_js:
  - "10"
cache: npm
branches:
  only:
    - master
before_script:
  - "npm run global add firebase-tools"
  - "npm run global add gatsby"
script:
  - "npm run build"
after_success:
  - "firebase deploy --token=${FIREBASE_API_TOKEN}"
